spring:
  application:
    name: people-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://${DS_URL}:${DS_PORT}/${DS_DB}
    username: ${DS_USERNAME}
    password: ${DS_PASSWORD}

